# visualizador-perfil-github

Endpointers que vamos usar

Perfil: GET/ users/:username

Reposit´[orioos: GET /users/:username/repos (usar query per_page=10 e sort=updated/ sort=pushed conforme objetivo)
